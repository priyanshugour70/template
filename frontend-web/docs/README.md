# docs/

Hand-written architectural notes, runbooks, and onboarding guides for this frontend.

Suggested files:

- `THEMING.md` — palette anatomy, adding a new palette, dark-mode pairing.
- `RBAC.md` — permission constants and how `usePermissions()` resolves them.
- `API_PROXY.md` — request lifecycle: browser → /api/v1/* → backend.
- `RUNBOOK.md` — common production incidents (Amplify build failures, env var drift).
