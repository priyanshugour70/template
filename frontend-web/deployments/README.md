# deployments/

Deployment artifacts for the frontend.

| Folder | Purpose |
|--------|---------|
| `docker/` | Alternate Dockerfile variants (multi-arch, prod-only, etc.). The default `Dockerfile` at repo root is the one referenced by `make build` and CI. |
