# src/constants/

Static constants without behaviour: regex patterns, enum-like maps, magic numbers that need a name.

Don't put data sets here (use `src/data/`) and don't put env-dependent values (use `src/config/`).
