# src/lib/session/

Lightweight wrapper over `lib/cookies` to make session reads/writes call-site-readable. Use from services that need to know whether a user is signed in without pulling in React context.
