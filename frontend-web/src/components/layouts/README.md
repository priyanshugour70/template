# src/components/layouts/

Per-route-group app shells. Each subfolder hosts a layout used by the corresponding `(group)/layout.tsx`.

- `dashboard/` — authenticated app shell (sidebar, top bar, user menu).
- `public/` — marketing/storefront shell (transparent header, hero containers).
- `back-office/` — admin shell (denser navigation, restricted by RBAC).
