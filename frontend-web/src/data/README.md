# src/data/

Hard-coded reference data sets that ship with the app: countries, currencies, time zones, ICAO codes, anything you'd otherwise hardcode inline.

Keep each file pure (no React, no async, no network calls).
