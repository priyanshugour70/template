# app/(dashboard)/

Authenticated app shell. All routes in this group require a valid session (enforced by `middleware.ts`).

Add a `layout.tsx` here for the sidebar + top bar shared by every dashboard route.
