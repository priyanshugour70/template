# migrations/postgres/

PostgreSQL schema migrations. Applied in lexicographic order by `scripts/migrate.sh`, recorded in `schema_migrations`.

See [`../README.md`](../README.md) for conventions. Replace `001_init.sql` with your real first migration.
